<h1>Create A New Trip</h1> 
<%= form_for :trip, url: user_trips_path do |f| %>
   
<h2>Choose A State</h2>
    <%= f.collection_select :state, State.all, :id, :name %> 
    <br/>
    <br/>
    <%= link_to "Select", states_path %>

    <%= f.submit "Select"  %>
<% end %>

